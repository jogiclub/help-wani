; 파일 위치: docker/web/php.ini
; 역할: 포털 PHP 런타임 설정
display_errors = On
error_reporting = E_ALL
log_errors = On
error_log = /dev/stderr
; 저장과 계산은 UTC 로 한다. 화면 표시만 조직 타임존으로 변환한다.
date.timezone = UTC
upload_max_filesize = 8M
post_max_size = 8M
session.cookie_httponly = 1
session.cookie_samesite = Lax
